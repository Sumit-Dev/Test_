/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ItmeList from './lib/ItmeList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ItmeList);
